import React from "react"
import ReactDOM from "react-dom"

fundtion Mymethod(){
  return(
      <ul>
      <li>Nices Beach</li>
      </ul>
  );
}

reactDOM.render(<Mymethod \>,element.getElementsById('root'))
